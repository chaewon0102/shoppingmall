<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.shoppingmall.repository.MembersMapper">
<select id="findByMemberId" parameterType="String" resultType="com.shop.shoppingmall.members.Members">
    SELECT * FROM toy_members WHERE member_id = #{memberId}
</select>
</mapper>